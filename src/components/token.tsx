export default () => {
  return (
    <span className="flex w-full flex-row justify-between">
      <h3 className="text-lg">token name</h3>
      <p>created on:</p>
      <button className="btn-error btn">Remove Token</button>
    </span>
  );
};
